<h1><%= @photo.title %></h1>

<br /> <br />
<% if @photo.image.attached? %>
  <div class="row">
    <div class="col">
      <h5>Original</h5>
      <%= image_tag @photo.image, class: "img-thumbnail" %>
      <div>
        Breite: <%= @photo.image_width %>
        Höhe: <%= @photo.image_height %>
      </div>
    </div>

    <div class="col">
      <h5>In Farbe</h5>
      <%= image_tag @photo.resize, class: "img-thumbnail" %>
    </div>

    <div class="col">
      <h5>Schwarzweiss 1. Variante</h5>
      <%= image_tag @photo.grayscale_variant, class: "img-thumbnail" %>
    </div>

    <div class="col">
      <h5>Schwarzweiss 2. Variante</h5>
      <%= image_tag @photo.grayscale_variant, class: "img-thumbnail" %>
    </div>

    <div class="col">
      <h5>nach rechts gekippt mit grünem Hintergrund</h5>
      <%= image_tag @photo.rotate_right, class: "img-thumbnail" %>
    </div>

    <div class="col">
      <h5>nach rechts gekippt mit transparentem Hintergrund</h5>
      <%= image_tag @photo.rotate_right_transparent, class: "img-thumbnail" %>
    </div>
  </div>
<% else %>
  Es wurde hierzu kein Foto hochgeladen
<% end %>

<div class="mt-4">
  <h5>Histogram (coming soon)</h5>
  <p>This is where we’ll generate and show a histogram of the B/W image.</p>
</div>

<%#= link_to "Foto bearbeiten", edit_photo_path(@photo) %>
<%= link_to "Zurück zur Übersicht", photos_path %>




<!-- <% if @photo.image.attached? %>
  <p>Original:</p>
  <%= image_tag @photo.image.variant(resize_to_limit: [500, 500]) %>
<% else %>
  <p>No image uploaded.</p>
<% end %> -->






